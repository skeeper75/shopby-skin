---
title: "auth-shop"
description: "인증(auth) 관련 shop API입니다"
version: "1.0"
category: "Shop API"
---

# auth-shop

:::info API 소개
 인증(auth) 관련 shop API입니다
:::

## 서버 정보

| 환경 | Base URL |
|------|----------|
| Production | `https://shop-api.shopby.co.kr` |

## 리소스 목록

| 리소스 | 설명 |
|--------|------|
| **Authentication** | 인증 |
| **Captcha** | 캡챠 |
| **KCPCertification** | KCP 본인인증 |
| **OAuthCallback** | Oauth 인증 |

## API 엔드포인트

## Authentication — 인증

### 🟢 `GET` `/authentications`

**인증번호 확인하기**

> operationId: `get-authentications`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `type` | query | ✅ | `string` | "인증 타입 (SMS: SMS 인증, EMAIL: 이메일 인증)" |
| `usage` | query | ✅ | `string` | "사용 용도 (FIND_ID: 아이디 찾기, FIND_PASSWORD: 비밀번호 찾기, RELEASE_DORMANT:\ |
| `certificatedNumber` | query | ✅ | `string` | 인증 번호 |
| `memberNo` | query | - | `number` | 회원 번호 |
| `notiAccount` | query | - | `string` | 이메일 주소 | 휴대폰 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/authentications`

**인증번호 발송하기**

> operationId: `SendAuthenticationNumber`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/authentications/email`

**이메일로 전달받은 인증코드 확인하기**

> operationId: `get-authentications-email`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `usage` | query | ✅ | `string` | "사용 용도 (FIND_ID: 아이디 찾기, FIND_PASSWORD: 비밀번호 찾기, RELEASE_DORMANT:\ |
| `email` | query | ✅ | `string` | 이메일 주소 |
| `certificatedNumber` | query | ✅ | `string` | 인증 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/authentications/email`

**이메일로 인증번호 발송하기**

> operationId: `post-authentications-email`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/authentications/sms`

**SMS로 전달받은 인증코드 확인하기**

> operationId: `get-authentications-sms`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `usage` | query | ✅ | `string` | "사용 용도 (FIND_ID: 아이디 찾기, FIND_PASSWORD: 비밀번호 찾기, RELEASE_DORMANT:\ |
| `mobileNo` | query | ✅ | `string` | 휴대전화 번호 |
| `key` | query | ✅ | `string` | 인증 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/authentications/sms`

**휴대폰 점유 인증을 위해 인증코드(6자리) SMS 발송하기**

> operationId: `post-authentications-sms`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/oauth/login-url`

**OpenId 로그인 url 조회하기**

> operationId: `get-oauth-login-url`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `provider` | query | ✅ | `string` | "간편 로그인 제공사(ncp_naver, ncp_kakao, ncp_kakao-sync, ncp_line, ncp_facebook,\ |
| `redirectUri` | query | ✅ | `string` | redirect URL |
| `state` | query | - | `string` | CSRF 공격을 방지하기 위해 애플리케이션에서 생성한 상태 토큰 |
| `reauthenticate` | query | - | `string` | 간편 로그인 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/oauth/openid`

**OpenId AccessToken 발급하기 - POST 방식**

> operationId: `post-oauth-openid`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `trackingKey` | query | - | `string` | 쇼핑채널링-추적키 |
| `reauthenticate` | query | - | `string` | 재인증 여부 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

## 부가설명 및 특이사항
OpenId 회원의 AccessToken 발급하기 위한 post 방식의 API\
        \ 입니다.

* 자동 로그인을 위해 **keepLogin**을 **true**로 요청하면 유효 기간이 90일인 토큰이 생성됩니다\
        .
* 유효 기간이 길기 때문에 토큰 탈취시 보안에 취약할 수 있습니다.

외부 IdP(Identity Provider, 아이디\
        \ 제공자)를 이용하는 회원이 로그인할 때 사용합니다.

지원하는 IdP 크게 (1) 간편 로그인을 지원하는 IdP, (2) 자체\
        \ 연동형 IdP(자체적으로 회원 서비스를 지원하는 경우 사용)로 나뉩니다.

## (1) 간편 로그인
각 개발자센터에서 쇼핑몰\
        에 맞는 올바른 설정을 먼저 진행해야합니다. 

[간편 로그인 가이드](https://workspace-help.nhn-commerce.com/aurora-skinguide/api-1/open-id)
\
        
## 간편 로그인 유형별 필수값

| provider | code  | redirectUri | state |
| :------:\
        \ | :---: | :---: | :---: |
| ncp_naver    |   O   |   X   |   △   |
| ncp_kakao\
        \    |   O   |   O   |   X   |
| ncp_kakao-sync    |   O   |   X   |   △\
        \   |
| ncp_line     |   O   |   O   |   O   |
| ncp_facebook |   O   |\
        \   O   |   X   |
| ncp_payco    |   O   |   X   |   △   |
| ncp_apple \
        \   |   O   |   △   |   △   |
| ncp_google    |   O   |   O   |   X   |
\
        | app-card    |   O   |   X   |   O   |

## App Card 로그인의 경우, code 에 trnasNo,\
        \ state 에 token 을 전달해야합니다.

## 참고

- [네이버 개발자센터](https://developers.naver.com/main)
\
        - [카카오 개발자센터](https://developers.kakao.com/)
- [라인 개발자센터](https://developers.line.biz/en/)
\
        - [페이스북 개발자센터](https://developers.facebook.com/)
- [페이코 개발자센터](https://developers.payco.com/)
\
        - [애플 개발자센터](https://developer.apple.com/)
- [구글 개발자센터](https://developers.google.com/)
\
        
## (2) 자체 회원 연동 (엔터프라이즈 전용)
자세한 사항은 아래 가이드와 서비스 담당자에게 문의해주시길 바랍니다.

\
        [자체회원 연동 가이드](https://workspace-help.nhn-commerce.com/recommendedcontents/contents/prm_member_guide)
\
        
## 자체 회원 연동 필수값

| provider | openAccessToken |
| :------: | :---: |
\
        | ncpstore |  O   |

---

### 🔴 `DELETE` `/oauth/openid`

**SNS 연동 해제하기**

> operationId: `delete-oauth-openid`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `provider` | query | ✅ | `string` | "간편 로그인 제공사(ncp_naver, ncp_kakao, ncp_kakao-sync, ncp_line, ncp_facebook,\ |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/oauth/token`

**AccessToken 발급하기**

> operationId: `post-oauth-token-dormant`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `trackingKey` | query | - | `string` | 쇼핑채널링-추적키 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔴 `DELETE` `/oauth/token`

**AccessToken 반환하기**

> operationId: `RevokeToken`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/oauth/openid/link`

**SNS 연동하기**

> operationId: `link-oauth-openid`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `provider` | query | ✅ | `string` | "간편 로그인 제공사(ncp_naver, ncp_kakao, ncp_kakao-sync, ncp_line, ncp_facebook,\ |
| `code` | query | - | `string` | "IdP(Identity Provider, 아이디 제공자)로부터 받은 인증코드(authorization code)\ |
| `openAccessToken` | query | - | `string` | "IdP(Identity Provider, 아이디 제공자) 엑세스 토큰 - 연동형 IdP(Identity Provider,\ |
| `redirectUri` | query | - | `string` | 개발자센터에 등록된 redirect URI |
| `state` | query | - | `string` | CSRF 공격을 방지하기 위해 애플리케이션에서 생성한 상태 토큰 |
| `keepLogin` | query | - | `string` | 자동로그인 여부(**true**로 요청하면 유효 기간이 90일인 토큰이 생성) |
| `platformType` | query | - | `string` | "클라이언트 플랫폼(PC, MOBILE_WEB, AOS, IOS)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

## 부가설명 및 특이사항
일반 회원에 SNS 연동을 하는 API 입니다.

외부 IdP(Identity\
        \ Provider, 아이디 제공자)를 이용하는 회원이 로그인할 때 사용합니다.

## 간편 로그인
각 개발자센터에서 쇼핑몰에\
        \ 맞는 올바른 설정을 먼저 진행해야합니다. 

[간편 로그인 가이드](https://workspace.nhn-commerce.com/guide/skin/dev-cover/easy-login?lv=11)
\
        
## 간편 로그인 유형별 필수값

| provider | code  | redirectUri | state |
| :------:\
        \ | :---: | :---: | :---: |
| ncp_naver    |   O   |   X   |   △   |
| ncp_kakao\
        \    |   O   |   O   |   X   |
| ncp_kakao-sync    |   O   |   X   |   △\
        \   |
| ncp_line     |   O   |   O   |   O   |
| ncp_facebook |   O   |\
        \   O   |   X   |
| ncp_payco    |   O   |   X   |   △   |
| ncp_apple \
        \   |   O   |   △   |   △   |
| ncp_google    |   O   |   O   |   X   |
\
        
## 참고

- [네이버 개발자센터](https://developers.naver.com/main)
- [카카오 개발자센터\
        ](https://developers.kakao.com/)
- [라인 개발자센터](https://developers.line.biz/en/)
\
        - [페이스북 개발자센터](https://developers.facebook.com/)
- [페이코 개발자센터](https://developers.payco.com/)
\
        - [애플 개발자센터](https://developer.apple.com/)
- [구글 개발자센터](https://developers.google.com/)
\

---

### 🟢 `GET` `/oauth/openid/app-card/qr`

**앱카드 QR Code 생성하기**

> operationId: `get-app-card-qr-code`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `transNo` | query | ✅ | `string` | 거래번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/oauth/openid/app-card/trans-no`

**앱카드 거래번호 및 토큰 발급하기**

> operationId: `get-app-card-trans-no`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/openid/token`

**AccessToken 조회하기**

> operationId: `get-openid-token`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

## Captcha — 캡챠

### 🟢 `GET` `/captcha/image`

**캡챠 이미지 생성하기**

> operationId: `get-captcha-image`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `key` | query | ✅ | `string` | 캡챠 구분키 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔵 `POST` `/captcha/verify`

**캡챠 인증코드 확인하기**

> operationId: `post-captcha-verify`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `code` | query | ✅ | `string` | 캡챠 인증코드 |
| `key` | query | ✅ | `string` | 캡챠 구분키 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

## KCPCertification — KCP 본인인증

### 🔵 `POST` `/kcp/age-verification`

**회원 성인인증하기**

> operationId: `post-kcp-age-verification`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `key` | query | ✅ | `string` | 본인인증 키 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/kcp/id-verification/form`

**KCP 본인인증 요청하기**

> operationId: `get-kcp-id-verification-form`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `returnUrl` | query | ✅ | `string` | 본인인증 완료 후 호출할 클라이언트 페이지 |
| `domestic` | query | - | `string` | "내국인 여부 (true : 내국인, false : 외국인 , null : 내국인 전용 QR 신청 본인인증)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🟢 `GET` `/kcp/id-verification/response`

**KCP 본인인증 결과 조회하기**

> operationId: `get-kcp-id-verification-response`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `key` | query | ✅ | `string` | 본인인증 키 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

## OAuthCallback — Oauth 인증

### 🟢 `GET` `/oauth/begin`

**OAuth 로그인 페이지 열기**

> operationId: `get-oauth-begin`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `clientId` | query | ✅ | `string` | API 사용을 위한 클라이언트 아이디 |
| `provider` | query | ✅ | `string` | "간편 로그인 제공사(ncp_naver, ncp_kakao, ncp_kakao-sync, ncp_line, ncp_facebook,\ |
| `nextUrl` | query | - | `string` | 로그인 성공 이후 로드할 페이지 주소 (callback URL) |
| `platform` | query | - | `string` | "클라이언트 플랫폼(PC, MOBILE_WEB, AOS, IOS)" |
| `state` | query | - | `string` | CSRF 공격을 방지하기 위해 애플리케이션에서 생성한 상태 토큰 |

## 부가설명 및 특이사항
간편 로그인을 위한 로그인 URL을 조회한 후 해당 주소로 리다이렉트하는 API\
        \ 입니다.
 
* /oauth/begin -> 간편 로그인 페이지 -> /oauth/callback -> nextUrl 순으로\
        \ 리다이렉트됩니다.
 
* nextUrl을 이용하여 로그인 성공 후 돌아갈 callback URL을 설정할 수 있습니다.
 
\
        * 쇼핑몰 운영자가 기본 도메인을 등록한 경우 /로 시작하는 상대 주소를 사용할 수 있고, 그 외의 경우에는 올바른 HTTP URL을\
        \ 전달해야 합니다.

---

### 🟢 `GET` `/oauth/callback`

**OAuth callback 처리하기(AccessToken 발급하기)**

> operationId: `get-oauth-callback`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `clientId` | query | ✅ | `string` | 클라이언트 아이디 |
| `provider` | query | ✅ | `string` | OAuth2 프로바이더 |
| `code` | query | ✅ | `string` | 인증 코드 |
| `state` | query | - | `string` | CSRF 공격을 방지하기 위해 애플리케이션에서 생성한 상태 토큰 |
| `nextUrl` | query | ✅ | `string` | 로그인 성공 이후 로드할 페이지 주소 (callback URL) |

## 부가설명 및 특이사항
OpenId 회원의 AccessToken 발급하기 위한 API 입니다.
 
\
        * /oauth/begin -> 간편 로그인 페이지 -> /oauth/callback -> nextUrl 순으로 리다이렉트됩니다.
\
        \ 
* 간편 로그인 성공후 받은 인증 코드를 통해 회원 토큰을 발급하고, 리다이렉트될 nexUrl의 쿼리에 토큰 정보를 담아 전달\
        합니다.
 
* ex) https://test.shopby.co.kr?accessToken=test-access-token&expireIn=3599&daysFromLastPasswordChange=17
\
        \ 
* 쿼리 파라미터는 /oauth/begin에서 정의한 값이 간편 로그인 페이지를 통해 넘어와야하며, 임의로 변경해서는 안됩니다\
        .

---

### 🟢 `GET` `/oauth/openid/list`

**SNS 연동 내역 조회**

> operationId: `get-oauth-list`

---

## OAUTH2

### 🟡 `PUT` `/oauth2`

**토큰 갱신하기**

> operationId: `put-oauth2-token`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |
| `Shop-By-Authorization` | header | ✅ | `string` | Authorization 헤더 |
| `Refresh-Token` | header | ✅ | `string` | Refresh-Token 헤더 |

---

### 🔵 `POST` `/oauth2`

**토큰 발급하기**

> operationId: `post-oauth2-token`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

---

### 🔴 `DELETE` `/oauth2`

**토큰 만료하기**

> operationId: `delete-oauth2-token`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |
| `Shop-By-Authorization` | header | ✅ | `string` | Authorization 헤더 |

---

### 🟡 `PUT` `/oauth2/openid`

**오픈아이디 토큰 재인증 처리하기**

> operationId: `put-oauth2-openid-reauthenticate`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |
| `Shop-By-Authorization` | header | ✅ | `string` | Authorization 헤더 |
| `Refresh-Token` | header | ✅ | `string` | Refresh-Token 헤더 |

---

### 🔵 `POST` `/oauth2/openid`

**오픈아이디 토큰 발급하기**

> operationId: `post-oauth2-openid-token`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "클라이언트 플랫폼 (PC, MOBILE_WEB, AOS, IOS)" |
| `language` | header | - | `string` | "언어 (기본값: ko)" |

## 부가설명 및 특이사항
* OpenId 회원의 AccessToken 발급하기 위한 post 방식의 API\
        \ 입니다.

* 회원 엑세스 토큰의 기본 유효 기간은 30 분이며,
* 회원 리프레시 토큰의 기본 유효 기간은 1 일 입니다\
        .,
* **keepLogin**을 **true**로 요청하면 리프레시 토큰 유효 기간을 90 일인 토큰이 생성됩니다.

외부\
        \ IdP(Identity Provider, 아이디 제공자)를 이용하는 회원이 로그인할 때 사용합니다.

지원하는 IdP 크게 (1)\
        \ 간편 로그인을 지원하는 IdP, (2) 자체 연동형 IdP(자체적으로 회원 서비스를 지원하는 경우 사용)로 나뉩니다.

##\
        \ (1) 간편 로그인
각 개발자센터에서 쇼핑몰에 맞는 올바른 설정을 먼저 진행해야합니다. 

[간편 로그인 가이드](https://workspace.nhn-commerce.com/guide/skin/dev-cover/easy-login?lv=11)
\
        
## 간편 로그인 유형별 필수값

| provider | code  | redirectUri | state |
| :------:\
        \ | :---: | :---: | :---: |
| ncp_naver    |   O   |   X   |   △   |
| ncp_kakao\
        \    |   O   |   O   |   X   |
| ncp_kakao-sync    |   O   |   X   |   △\
        \   |
| ncp_line     |   O   |   O   |   O   |
| ncp_facebook |   O   |\
        \   O   |   X   |
| ncp_payco    |   O   |   X   |   △   |
| ncp_apple \
        \   |   O   |   △   |   △   |
| ncp_google    |   O   |   O   |   X   |
\
        
## 참고

- [네이버 개발자센터](https://developers.naver.com/main)
- [카카오 개발자센터\
        ](https://developers.kakao.com/)
- [라인 개발자센터](https://developers.line.biz/en/)
\
        - [페이스북 개발자센터](https://developers.facebook.com/)
- [페이코 개발자센터](https://developers.payco.com/)
\
        - [애플 개발자센터](https://developer.apple.com/)
- [구글 개발자센터](https://developers.google.com/)
\
        
## (2) 자체 회원 연동 (엔터프라이즈 전용)
자세한 사항은 아래 가이드와 서비스 담당자에게 문의해주시길 바랍니다.

\
        [자체회원 연동 가이드](https://workspace.nhn-commerce.com/support/recommendedContents/23046)
\
        
## 자체 회원 연동 필수값

| provider | openAccessToken |
| :------: | :---: |
\
        | ncpstore |  O   |

---

