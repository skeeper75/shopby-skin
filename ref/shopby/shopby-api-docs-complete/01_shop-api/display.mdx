---
title: "display-shop"
description: "상품 전시(display) 관련 shop API입니다."
version: "0.0.1-SNAPSHOT"
category: "Shop API"
---

# display-shop

:::info API 소개
 상품 전시(display) 관련 shop API입니다.
:::

## 서버 정보

| 환경 | Base URL |
|------|----------|
| Production | `https://shop-api.e-ncp.com` |

## 리소스 목록

| 리소스 | 설명 |
|--------|------|
| **Banner** | 배너 |
| **Category** | 카테고리 |
| **Review** | 상품평 |
| **Event** | 기획전 |
| **Popup** | 팝업 |
| **ProductSection** | 상품 진열 관리 |
| **ProductInquiry** | 상품 문의 |

## API 엔드포인트

## Category — 카테고리

### 🟢 `GET` `/categories`

**전체 카테고리 조회하기**

> operationId: `get-categories-by-keyword`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keyword` | query | - | `string` | 카테고리명 |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
키워드로 카테고리를 조회하는 API입니다

* 계층을\
        \ 가지는 카테고리 목록(multiLevelCategories)을 조회합니다 (최대 5 depth)

* 원본 카테고리 목록(flatCategories)을\
        \ 조회합니다

1일 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/categories/new-product-categories`

**신상품이 있는 카테고리 조회하기**

> operationId: `get-new-product-categories`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
판매 시작일이 1주일 이내인 상품이 존재하는 카테고리 조회\
        하는 API입니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/categories/search-by-management-code`

**관리코드로 카테고리 번호를 조회하기**

> operationId: `get-category-nos-by-codes`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
전시카테고리 관리코드로 카테고리 번호를 조회하는 API 입\
        니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/categories/simple-1depth`

**1차 카테고리 간단 정보 조회하기**

> operationId: `get-categories-simple-1depth`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
1차 카테고리 관련 간단한 정보를 조회하는 API 입니다
\
        
 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/categories/{categoryNo}`

**카테고리 조회하기**

> operationId: `get-category`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `categoryNo` | path | ✅ | `string` | 카테고리 번호 |
| `needsBrands` | query | - | `boolean` | "브랜드 정보 조회 여부 (default: true)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
카테고리 번호로 카테고리를 조회하는 API입니다

*\
        \ 계층을 가지는 카테고리 목록(multiLevelCategories)을 조회합니다 (최대 5 depth)

* 원본 카테고리 목\
        록(flatCategories)을 조회합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## Review — 상품평

### 🟢 `GET` `/category/product-reviews`

**카테고리 상품평 목록 조회하기**

> operationId: `get-category-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `hasAttachmentFile` | query | - | `boolean` | "첨부 파일 여부 (default : false)" |
| `categoryDepth` | query | ✅ | `number` | 카테고리 깊이 |
| `categoryNo` | query | ✅ | `number` | 카테고리 번호 |
| `productName` | query | - | `string` | 상품명 |
| `order.by` | query | ✅ | `string` | "정렬 기준 (추천순: RECOMMEND, 등록일순: REGISTER_YMDT, 평점순: RATING, 베스트\ |
| `order.direction` | query | ✅ | `string` | "정렬 순서 (default: DESC) (오름차순: ASC, 내림차순: DESC)" |
| `brandName` | query | - | `string` | 브랜드 명 |
| `bestReviewYn` | query | - | `string` | "베스트 상품평 여부 ( 우수상품평:Y , 일반상품평:N , 전체: NULL - default)" |
| `myReviewYn` | query | - | `string` | "내 상품평 보기 ( 내상품평:Y , 전체상품평:N - default )" |
| `pageNumber` | query | ✅ | `number` | 페이지 번호 |
| `pageSize` | query | ✅ | `number` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasOrderedOption` | query | - | `boolean` | "주문 옵션 정보 포함 여부(default: true)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
카테고리 별 상품평 목록 조회하는 API입니다

###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

Paging\
        \ 기능 제공합니다

정렬 기준(모든 정렬은 1차적으로 정렬된 후, 최근 등록된 순으로 한 번 더 정렬됩니다.)

* RATING\
        \ - 상품평 평점순

* RECOMMEND - 상품평 추천순

* REGISTER_YMDT - 상품평 등록순

* BEST_REVIEW\
        \ - 우수 상품평순(DESC: 우수 상품평 먼저 조회, ASC: 일반 상품평 먼저 조회)

hasOrderedOption 을 false로\
        \ 설정 했을 때, orderedOption 항목은 디폴트 옵션 정보 값으로 내려갑니다. (default: false)

* 주문\
        \ 옵션 정보 필요한 경우 hasOrderedOption=true 로 설정하여 요청하시기 바랍니다.
## 화면 예시
      \
        \          
[![IMG_CATEGORY_REVIEW_GET](https://image.toast.com/aaaaahb/api-description/product/%EC%83%81%ED%92%88%ED%8F%89/[GET]%20category_product-reviews%20%EC%B9%B4%ED%85%8C%EA%B3%A0%EB%A6%AC%20%EC%83%81%ED%92%88%ED%8F%89%20%EB%AA%A9%EB%A1%9D.png?autox150)](http://image.toast.com/aaaaahb/api-description/product/%EC%83%81%ED%92%88%ED%8F%89/[GET]%20category_product-reviews%20%EC%B9%B4%ED%85%8C%EA%B3%A0%EB%A6%AC%20%EC%83%81%ED%92%88%ED%8F%89%20%EB%AA%A9%EB%A1%9D.png)

---

### 🟢 `GET` `/product-reviews/configurations`

**상품평 게시판 설정 조회하기**

> operationId: `get-product-reviews-configurations`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | - | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 게시판 설정을 조회하는 API입니다

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/photo-reviews`

**상품의 포토 후기 목록 조회하기**

> operationId: `get-products-photo-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | "" |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부 (default: false)" |
| `tagValueNos` | query | - | `string` | 태그값번호 |
| `pageNumber` | query | - | `number` | "페이지 번호 (default: 1)" |
| `pageSize` | query | - | `number` | "한 페이지당 노출 수 (default: 10)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 별 포토 상품평 목록을 조회하는 API입니다

\
        Paging 기능 제공합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/product-reviews`

**상품평 목록 조회하기**

> operationId: `get-product-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `hasAttachmentFile` | query | - | `boolean` | "첨부 파일 여부 ( 파일 첨부:Y, 파일 미첨부(default):N, 빈값:ALL )" |
| `order.by` | query | - | `string` | "정렬 기준 (추천순: RECOMMEND, 등록일순: REGISTER_YMDT, 평점순: RATING, 베스트\ |
| `order.direction` | query | - | `string` | "정렬 순서 (default: DESC) (오름차순: ASC, 내림차순: DESC)" |
| `bestReviewYn` | query | - | `string` | "베스트 상품평 여부 ( 우수상품평:Y , 일반상품평:N , 전체: NULL )" |
| `pageNumber` | query | - | `number` | "페이지 번호 (default : 1)" |
| `pageSize` | query | - | `number` | "한 페이지당 노출 수, 페이지 사이즈는 최대 100건까지 가능합니다.(default: 10)" |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasProductInfo` | query | - | `boolean` | "상품 정보 포함 여부(default: true)" |
| `hasReviewSummary` | query | - | `boolean` | "상품평 점수별 리뷰 수 정보 포함 여부(default: true)" |
| `hasOrderedOption` | query | - | `boolean` | "주문 옵션 정보 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 별 상품평 목록을 조회하는 API입니다

Paging\
        \ 기능 제공합니다

정렬 기준(모든 정렬은 1차적으로 정렬된 후, 최근 등록된 순으로 한 번 더 정렬됩니다.)

* RATING\
        \ - 상품평 평점순

* RECOMMEND - 상품평 추천순

* REGISTER_YMDT - 상품평 등록순

* BEST_REVIEW\
        \ - 우수 상품평순(DESC: 우수 상품평 먼저 조회, ASC: 일반 상품평 먼저 조회)

hasProductInfo 를 false\
        \ 로 설정하였을때 다음의 항목은 빈값으로 내려갑니다. (default: true)

* productName, productNameEn,\
        \ memberGradeDisplayInfo, memberGroupDisplayInfo, isDeletedProductReview
\
        
* brandName, brandNameEn, imageUrl, imageUrlType, orderedOption, productDiscountPrice
\
        
* 상품정보를 이미 조회하여 알고 있는 경우 hasProductInfo=false 로 설정하여 요청하시기 바랍니다.

hasReviewSummary\
        \ 를 false 로 설정하였을때 다음의 항목은 빈값으로 내려갑니다. (default: true)

* reviewRatingResponses
\
        
* 해당 항목은 리뷰 점수별 리뷰수를 나타내주는 정보로 5점 15개, 4점 10개, 3점 5개, 2점 1개, 1점 1개 처럼 보여\
        주고 싶은경우 사용합니다.

* 해당정보가 필요없는경우 hasReviewSummary=false 로 설정하여 요청하시기 바랍니다\
        .

hasOrderedOption 을 false로 설정 했을 때, orderedOption 항목은 디폴트 옵션 정보 값으로 내려\
        갑니다. (default: false)

* 주문 옵션 정보 필요한 경우 hasOrderedOption=true 로 설정하여 요청\
        하시기 바랍니다.

페이지 사이즈는 최대 100건까지 가능합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/product-reviews`

**상품평 등록하기**

> operationId: `post-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 등록하는 API입니다

 ### OAUTH2 참\
        고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/reviewable-options`

**상품평 가능 유무 조회하기**

> operationId: `get-products-reviewable-options`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `orderNo` | query | - | `string` | 주문 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평을 작성할 수 있는 구매확정 상품옵션 조회하는 API입\
        니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/product-reviews/`

**상품평 목록 조회하기 2.0**

> operationId: `get-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `hasAttachmentFile` | query | - | `boolean` | "첨부 파일 여부 ( 파일 첨부:Y, 파일 미첨부(default):N, 빈값:ALL )" |
| `order.by` | query | - | `string` | "정렬 기준 (추천순: RECOMMEND, 등록일순: REGISTER_YMDT, 평점순: RATING, 베스트\ |
| `order.direction` | query | - | `string` | "정렬 순서 (default: DESC) (오름차순: ASC, 내림차순: DESC)" |
| `bestReviewYn` | query | - | `string` | "베스트 상품평 여부 ( 우수상품평:Y , 일반상품평:N , 전체: NULL )" |
| `pageNumber` | query | - | `number` | "페이지 번호 (default : 1)" |
| `pageSize` | query | - | `number` | "한 페이지당 노출 수, 페이지 사이즈는 최대 100건까지 가능합니다. (default: 10)" |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `ratingRange.from` | query | - | `number` | "리뷰평점 시작범위(default: null)" |
| `ratingRange.to` | query | - | `number` | "리뷰평점 끝범위(default: null)" |
| `tagValueNos` | query | - | `string` | 태그값번호 |
| `hasProductInfo` | query | - | `boolean` | "상품 정보 포함 여부(default: true)" |
| `hasOrderedOption` | query | - | `boolean` | "주문 옵션 정보 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 별 상품평 목록을 조회하는 API입니다

Paging\
        \ 기능 제공합니다

정렬 기준(모든 정렬은 1차적으로 정렬된 후, 최근 등록된 순으로 한 번 더 정렬됩니다.)

* RATING\
        \ - 상품평 평점순

* RECOMMEND - 상품평 추천순

* REGISTER_YMDT - 상품평 등록순

* BEST_REVIEW\
        \ - 우수 상품평순(DESC: 우수 상품평 먼저 조회, ASC: 일반 상품평 먼저 조회)

hasProductInfo 를 false\
        \ 로 설정하였을때 다음의 항목은 빈값으로 내려갑니다. (default: true)

* productName, productNameEn,\
        \ memberGradeDisplayInfo, memberGroupDisplayInfo, isDeletedProductReview
\
        
* brandName, brandNameEn, imageUrl, imageUrlType, orderedOption, productDiscountPrice
\
        
* 상품정보를 이미 조회하여 알고 있는 경우 hasProductInfo=false 로 설정하여 요청하시기 바랍니다.

hasOrderedOption\
        \ 을 false로 설정 했을 때, orderedOption 항목은 디폴트 옵션 정보 값으로 내려갑니다. (default: false)
\
        
* 주문 옵션 정보 필요한 경우 hasOrderedOption=true 로 설정하여 요청하시기 바랍니다.

페이지 사이즈는 최\
        대 100건까지 가능합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/product-reviews/summary`

**상품별 평점 구간별 매칭 리뷰수**

> operationId: `post-products-summary`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 리뷰평점 구간별 상품매칭수를 확인하는 API입니다
\
        
 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/product-reviews/{reviewNo}`

**상품평 가져오기**

> operationId: `get-products-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `hasOrderedOption` | query | - | `boolean` | "주문 옵션 정보 포함 여부(default: true)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 조회하는 API입니다

 ### OAUTH2 참\
        고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

hasOrderedOption\
        \ 을 false로 설정 했을 때, orderedOption 항목은 디폴트 옵션 정보 값으로 내려갑니다. (default: false)
\
        
* 주문 옵션 정보 필요한 경우 hasOrderedOption=true 로 설정하여 요청하시기 바랍니다.

---

### 🟡 `PUT` `/products/{productNo}/product-reviews/{reviewNo}`

**상품평 수정하기**

> operationId: `put-product-review`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 수정하는 API입니다

작성자만 수정 가능합니다\
        

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔴 `DELETE` `/products/{productNo}/product-reviews/{reviewNo}`

**상품평 삭제하기**

> operationId: `delete-product-review`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 삭제하는 API입니다

작성자만 삭제 가능합니다\
        

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/product-reviews/{reviewNo}/comments`

**상품평의 댓글 목록 조회하기**

> operationId: `get-products-product-reviews-comments`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | "" |
| `reviewNo` | path | ✅ | `string` | "" |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부 (default: false)" |
| `page` | query | - | `number` | "페이지 번호 (default: 1)" |
| `size` | query | - | `number` | "한 페이지당 노출 수 (default: 10)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 전체 목록을 조회하는 API입니다

Paging\
        \ 기능 제공합니다

정렬 기준(모든 정렬은 1차적으로 정렬된 후, 최근 등록된 순으로 한 번 더 정렬됩니다.)

* RATING\
        \ - 상품평 평점순

* RECOMMEND - 상품평 추천순

* REGISTER_YMDT - 상품평 등록순

* BEST_REVIEW\
        \ - 우수 상품평순(DESC: 우수 상품평 먼저 조회, ASC: 일반 상품평 먼저 조회)

 ### OAUTH2 참고사항
*\
        \ [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/product-reviews/{reviewNo}/recommend`

**상품평 추천하기**

> operationId: `post-product-reviews-recommend`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 추천하는 API입니다

 ### OAUTH2 참\
        고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔴 `DELETE` `/products/{productNo}/product-reviews/{reviewNo}/recommend`

**상품평 추천 취소하기**

> operationId: `delete-product-reviews-recommend`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 추천 취소하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/product-reviews/{reviewNo}/report`

**상품평 신고하기**

> operationId: `post-product-reviews-report`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 신고하는 API입니다

 ### OAUTH2 참\
        고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔴 `DELETE` `/products/{productNo}/product-reviews/{reviewNo}/report`

**상품평 신고 취소하기**

> operationId: `cancel-product-review-report`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `reviewNo` | path | ✅ | `string` | 상품평 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 삭제하는 API입니다

작성자만 삭제 가능합니다\
        

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/profile/product-reviews`

**내 상품평 목록 조회하기**

> operationId: `get-profile-product-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `pageNumber` | query | - | `integer` | 페이지 번호 |
| `pageSize` | query | - | `integer` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `startYmd` | query | - | `string` | "조회 시작일(yyyy-MM-dd), 미입력시 30일 전" |
| `endYmd` | query | - | `string` | "조회 종료일(yyyy-MM-dd), 미입력시 오늘 날짜" |
| `bestReviewYn` | query | - | `string` | "베스트 상품평 여부 ( 우수상품평:Y , 일반상품평:N , 전체: NULL )" |
| `searchType` | query | - | `string` | "검색어 기준 (내용: CONTENT, 상품명: PRODUCT_NAME, 전체: ALL)" |
| `searchKeyword` | query | - | `string` | 검색어 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
내 상품평 목록을 조회하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/profile/order-options/product-reviewable`

**내 상품평 작성 가능 목록 조회하기**

> operationId: `get-product-reviewable`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `pageNumber` | query | - | `integer` | 페이지 번호 |
| `pageSize` | query | - | `integer` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `startDate` | query | - | `string` | "조회 시작 일시, 미입력시 3개월 전" |
| `endDate` | query | - | `string` | "조회 종료 일시, 미입력시 오늘 날짜" |
| `productName` | query | - | `string` | 상품명 |
| `productNo` | query | - | `number` | 상품번호 |
| `orderNo` | query | - | `string` | 주문번호 |
| `onlyBuyConfirm` | query | - | `boolean` | "구매확정만 조회(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
내 상품평 작성 가능 목록들을 조회하는 API입니다

\
        \ ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/reviews/boards`

**상품평 게시판 목록 조회하기**

> operationId: `get-products-reviews-by-board`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sorting.sortCriterion` | query | - | `string` | "정렬 기준 (추천순: RECOMMEND, 등록일순: REGISTER_YMDT, 평점순: RATING, 베스트\ |
| `sorting.ordering` | query | - | `string` | "정렬 순서 (오름차순: ASC, 내림차순: DESC) (default : DESC)" |
| `depth1DisplayCategoryNo` | query | - | `number` | 전시 카테고리 번호 (1depthNo) |
| `keyword` | query | - | `string` | 검색어 |
| `pageNumber` | query | - | `number` | "페이지 번호 (default: 1)" |
| `boardType` | query | ✅ | `string` | "게시판 유형  (전체상품평: ALL, 포토상품평: PHOTO, 일반상품평: REVIEW)" |
| `isWidget` | query | - | `boolean` | "위젯 여부 (default : false)" |
| `tagValueNos` | query | - | `string` | 태그값번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품평 게시판 목록을 조회하는 API입니다 (전체 상품평\
        \ / 포토 상품평)

Paging 기능 제공합니다

page 사이즈는 몰의 설정된 값을 조회하여 자동으로 적용됩니다

\
        isWidget = true 인 경우, 입력받은 requestParameter 값이 아닌 위젯의 리뷰 노출 갯수와 베스트 후기 순으로\
        \ 조회됩니다.

일반 상품평(boardType: REVIEW)의 경우, '전체 상품후기(boardType: ALL)'의 게시판\
        \ 설정에 따라 조회됩니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/reviews/tags`

**상품리뷰 태그 전체 조회하기**

> operationId: `get-reviews-tags`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품리뷰 태그 전체 조회하는 API입니다.

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/reviews/boards/reviewed-products`

**상품 기준 상품평 게시판 목록 조회하기**

> operationId: `get-reviews-by-products`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sorting.sortCriterion` | query | - | `string` | "정렬 기준 (리뷰총개수: REVIEW_COUNT, 등록일: REGISTER_YMDT, 리뷰평점: REVIEW_RATE)\ |
| `sorting.ordering` | query | - | `string` | "정렬 순서 (오름차순: ASC, 내림차순: DESC) (default:DESC)" |
| `depth1DisplayCategoryNo` | query | - | `number` | 전시 카테고리 번호 (1depthNo) |
| `keyword` | query | - | `string` | 검색어 |
| `pageNumber` | query | - | `number` | "페이지 번호 (default: 1)" |
| `isWidget` | query | - | `boolean` | "위젯 여부 (default : false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 기준 상품평 게시판 목록을 조회하는 API입니다

\
        Paging 기능 제공합니다

page 사이즈는 몰의 설정된 값을 조회하여 자동으로 적용됩니다

isWidget = true\
        \ 인 경우, 입력받은 requestParameter 값이 아닌 위젯의 리뷰 노출 갯수와 리뷰가 많은 순으로 조회됩니다.

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## Popup — 팝업

### 🔵 `POST` `/design-popups`

**디자인 팝업 조회하기**

> operationId: `get-design-popup`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
노출페이지 설정에서 개별설정으로 선택한 경우만 해당됩니다\
        .

노출 URL 및 파라미터 조건이 있는 경우에만 필터링 후 가져옵니다

 ### OAUTH2 참고사항
* [Oauth2\
        \ API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/popups`

**전체 팝업 목록 조회하기**

> operationId: `get-popups`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `pageType` | query | - | `string` | "페이지 유형 (메인페이지: MAIN, 전시카테고리: CATEGORY, 기획전: EVENT, 상품: PRODUCT)" |
| `targetNo` | query | - | `integer` | 페이지 번호 (페이지 유형에 따른 페이지 번호) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
전체 팝업 목록 조회하는 API입니다

1일 캐시하여\
        \ 사용하고 있습니다. (cached)

전체 대상이 아닌 일부만 설정한 경우 targetNo 에 설정한 전시카테고리/지획전/상품\
        번호를 입력해야합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token
## 화면\
        \ 예시
                
[![POPUP_ALL_LIST_IMG](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups%20%EC%A0%84%EC%B2%B4%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups%20%EC%A0%84%EC%B2%B4%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png)

---

### 🟢 `GET` `/display/popups/{popupNos}`

**팝업 목록 조회하기**

> operationId: `get-popups-by-popup-nos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `popupNos` | path | ✅ | `string` | "조회 할 팝업 번호(,로 구분한 배열) / 샵바이 어드민에서 등록된 팝업 번호입니다" |
| `pageType` | query | - | `string` | "페이지 유형 (메인페이지: MAIN, 전시카테고리: CATEGORY, 기획전: EVENT, 상품: PRODUCT)" |
| `targetNo` | query | - | `integer` | 페이지 번호 (페이지 유형에 따른 페이지 번호) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
팝업 번호로 전체 팝업 목록 조회하는 API입니다

\
        \ ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token
## 화면\
        \ 예시
                
[![POPUP_LIST_IMG](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups_popupNos%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups_popupNos%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png)

---

### 🟢 `GET` `/display/popups/ids/{popupIds}`

**팝업 목록 ID로 조회하기**

> operationId: `get-popups-by-popup-ids`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `popupIds` | path | ✅ | `string` | "조회 할 팝업 아이디(,로 구분한 배열) / 샵바이 어드민에서 등록된 팝업 아이디입니다" |
| `pageType` | query | - | `string` | "페이지 유형 (메인페이지: MAIN, 전시카테고리: CATEGORY, 기획전: EVENT, 상품: PRODUCT)" |
| `targetNo` | query | - | `integer` | 페이지 번호 (페이지 유형에 따른 페이지 번호) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
팝업 ID로 전체 팝업 목록 조회하는 API입니다

\
        \ ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token
## 화면\
        \ 예시
                
[![POPUP_LIST_IMG](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups_popupNos%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_popups_popupNos%20%ED%8C%9D%EC%97%85%20%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C(cached).png)

---

## Event — 기획전

### 🟢 `GET` `/display/events`

**이벤트 기간안에 포함된 모든 이벤트 목록 조회하기**

> operationId: `get-events`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keyword` | query | - | `string` | 검색어 (태그 명) |
| `categoryNos` | query | - | `number` | 전시 카테고리 번호로 검색 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
이벤트 기간안에 포함된 모든 이벤트 목록 조회하는 API입\
        니다

1일 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/`

**기획전 조회하기 v2.0**

> operationId: `get-event-by-keyword-type`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keywordInfo.type` | query | - | `string` | "검색어 타입(NO - 기획전 번호, NAME - 기획전 이름, TAG - 태그, URL - 기획전 URL,\ |
| `keywordInfo.value` | query | - | `string` | "검색어 필수(기획전 번호, TAG 는 ',' 로 구분하여 다중입력) (공백없이 입력)" |
| `page.number` | query | ✅ | `number` | 페이지 번호 |
| `page.size` | query | ✅ | `number` | 한 페이지당 노출 수 |
| `categoryNos` | query | - | `number` | 전시 카테고리 번호로 검색 |
| `eventYn` | query | - | `string` | "이벤트 여부 - Y or N (default : null)" |
| `progressStatus` | query | - | `string` | "진행상태 (ING - 진행중, READY - 진행예정, END - 진행종료, ALL - 모두 포함) (default:\ |
| `order.by` | query | - | `string` | "정렬조건(REGISTER_DATE: 등록일, START_DATE: 시작일, END_DATE: 종료일) (default:\ |
| `order.direction` | query | - | `string` | "정렬 방식(DESC : 내림차순 / ASC : 오름차순) (default : DESC)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
기획전을 조회하는 API 입니다.

키워드 타입: 기획\
        전 이름(NAME), 기획전 번호(NO), 태그(TAG), 아이디(ID)

* 예시: 기획전 이름으로 조회 시 - keywordInfo.type(=NAME),\
        \ keywordInfo.value(=특별 할인)

* NAME 타입은 keywordInfo.value에 1가지 값으로 조회 가능\
        합니다.

* keywordInfo.value가 없는 경우에는 검색조건으로 사용되지 않습니다.

header의 'Version'\
        \ 값을 2.0으로 요청해야 정상 동작합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/close`

**종료된 모든 이벤트 목록 조회하기**

> operationId: `get-closed-events`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keyword` | query | - | `string` | 태그 검색 |
| `eventTitle` | query | - | `string` | 이벤트명 검색 |
| `pageNumber` | query | - | `number` | "페이지 번호 (default : 1)" |
| `pageSize` | query | - | `number` | "한 페이지당 노출 수 (default : 10)" |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 여부 (default : false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
종료된 모든 이벤트 목록 조회하는 API입니다

1일\
        \ 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/products`

**다수의 상품 번호로 이벤트 목록 조회하기**

> operationId: `get-events-by-product-nos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNos` | query | ✅ | `number` | 상품 번호 |
| `categoryNos` | query | - | `number` | 전시 카테고리 번호로 검색 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
다수의 상품 번호로 이벤트 목록 조회하는 API입니다

\
        * 예시. 10001, 10002 상품으로 조회 시, 10001상품이 포함된 기획전과 10002 상품이 포함된 기획전 모두 조회

\
        1일 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/search-by-name`

**기획전명으로 기획전 검색하기**

> operationId: `get-event-by-name`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keyword` | query | ✅ | `string` | 검색어 (기획전 명) |
| `categoryNos` | query | - | `number` | 전시 카테고리 번호로 검색 |
| `onlyIngStatus` | query | - | `boolean` | "진행중인 기획전만 검색 (default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
기획전명으로 기획전을 조회하는 API입니다 ( like 검\
        색 ) 

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/search-by-nos`

**기획전번호 리스트로 기획전 조회하기**

> operationId: `search-event-by-event-nos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventNos` | query | ✅ | `number` | "조회 대상 기획전번호 목록(\",\" 구분, 최소 1, 최대 10)" |
| `countPerEvent` | query | - | `string` | "기획전별 조회 대상 상품 개수, 최소 1, 최대 10(DEFAULT)" |
| `soldout` | query | - | `boolean` | "품절 상품 포함 여부(DEFAULT: false)" |
| `saleStatus` | query | - | `string` | "판매 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
기획전번호 리스트로 기획전을 조회하는 API 입니다.

\
        현재 진행중인 기획전만 조회되고, 그 이외(진행대기, 종료)에는 조회되지 않습니다.

플랫폼별, 멤버그룹별, 멤버등급별에 따라 접\
        근 불가능한 기획전은 조회되지 않습니다.

products: 상품 목록 응답

* 응답에서 기획전 내 상품목록은 전시순서가 가\
        장 앞선 1개의 섹션 상품 목록이 조회되며, 상품 전시 순서에 의해 정렬됩니다.

* 상품 목록은 countPerEvent 변수에\
        \ 의해 응답 상품 개수가 제한되며,

* 그 중 품절상품, 판매종료된 상품은 soldout, saleStatus 파라미터에 의해\
        \ 다시 제외될 수 있습니다.

* 예를 들어, 15개의 상품 목록은 먼저 `countPerEvent = 10`에 의해 10개 상\
        품으로 제한되고, 품절상품 및 판매 종료 상품이 제외되어 실제 결과가 10개 미만으로 표시될 수 있습니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/search-by-progress`

**"키워드별, 진행상태별로 기획전 검색하기"**

> operationId: `get-event-by-keyword-and-progress`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keyword` | query | ✅ | `string` | 검색어 (키워드 타입에 따라 태그 or 기획전명) |
| `keywordType` | query | - | `string` | "검색어 타입 (TAG, NAME / default: 모두 포함)" |
| `categoryNos` | query | - | `number` | 전시 카테고리 번호로 검색 |
| `progressStatus` | query | - | `string` | "진행상태 (ING - 진행중, READY - 진행예정, END - 진행종료, ALL / default: ING)\ |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
키워드별(기획전명 or 태그)와 진행상태별(진행중 or 진\
        행종료 or 진행예정)로 기획전을 조회하는 API입니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/sections`

**다수의 기획전 번호로 기획전 섹션 목록 조회하기**

> operationId: `get-sections-by-event-nos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventNos` | query | ✅ | `string` | 기획전 번호 리스트 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
다수의 기획전 번호로 기획전 상품진열 목록 조회하는 API입\
        니다.

현재 진행중인 기획전만 조회되고, 그 이외(진행대기, 종료)에는 조회되지 않습니다.

플랫폼별, 멤버그룹별, 멤버등급\
        별에 따라 접근 불가능한 기획전은 조회되지 않습니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/{eventNo}`

**기획전 상세 조회하기**

> operationId: `get-event`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventNo` | path | ✅ | `string` | 기획전 번호 |
| `order` | query | - | `string` | "정렬조건(SALE: 판매시작일 오름차순 - default, ADMIN_SETTING: 관리자 설정 순서, BEST_SELLER:\ |
| `soldout` | query | - | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | - | `string` | "판매 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `hasProductDetail` | query | - | `boolean` | "false 인 경우 상품정보를 내려주지 않고 상품 번호만 내려줍니다.(default: true)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
 ### 해당 API는 Deprecated 되고 향후 다른\
        \ API로 대체될 예정입니다.
 ### 변경 사항이 발생하면 공지를 통해 안내드리겠습니다.
 기획전을 조회하는 API입니다.
\
        
* 현재 진행중인 기획전만 조회되고, 그 이외(진행대기, 종료)에는 조회되지 않습니다.

* 플랫폼별, 멤버그룹별, 멤버등급별\
        에 따라 접근 불가능한 기획전은 조회되지 않습니다.

* 에러코드: EVEC0001(기획전이 존재하지 않는 경우), EVEC0002(현\
        재 진행중인 기획전이 아닌 경우), EVEC0003(접근할 수 없는 기획전인 경우)

* hasProductDetail 이 false\
        \ 인 경우 soldout, order, saleStatus 정보는 무시됩니다.

* hasProductDetail 이 false\
        \ 인 경우 section.products 의 정보는 emptyList 로 내려갑니다. section.productNos 에서 상품번\
        호만 확인 가능합니다.

* hasProductDetail 이 false 인 경우 section.productNos의 상품번호는\
        \ 상품상태, 판매기간, 회원등급/그룹, 플랫폼 구분을 체크하지 않고 기획전에 설정된 상품번호들을 내려줍니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/ids/{eventId}`

**기획전 ID로 상세 조회하기**

> operationId: `get-event-by-id`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventId` | path | ✅ | `string` | 기획전 ID |
| `order` | query | - | `string` | "정렬조건(SALE: 판매시작일 오름차순 - default, ADMIN_SETTING: 관리자 설정 순서, BEST_SELLER:\ |
| `soldout` | query | - | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | - | `string` | "판매 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `hasProductDetail` | query | - | `boolean` | "false 인 경우 상품정보를 내려주지 않고 상품 번호만 내려줍니다.(default: true)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
ID로 기획전을 조회하는 API입니다.

현재 진행중인\
        \ 기획전만 조회되고, 그 이외(진행대기, 종료)에는 조회되지 않습니다.

플랫폼별, 멤버그룹별, 멤버등급별에 따라 접근 불가능한\
        \ 기획전은 조회되지 않습니다.

에러코드: EVEC0001(기획전이 존재하지 않는 경우), EVEC0002(현재 진행중인 기획전\
        이 아닌 경우), EVEC0003(접근할 수 없는 기획전인 경우)

hasProductDetail 이 false 인 경우 soldout,\
        \ order, saleStatus 정보는 무시됩니다.

hasProductDetail 이 false 인 경우 section.products\
        \ 의 정보는 emptyList 로 내려갑니다. section.productNos 에서 상품번호만 확인 가능합니다.

hasProductDetail\
        \ 이 false 인 경우 section.productNos의 상품번호는 상품상태, 판매기간, 회원등급/그룹, 플랫폼 구분을 체크하지\
        \ 않고 기획전에 설정된 상품번호들을 내려줍니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/products/{productNo}`

**특정 상품을 포함하는 이벤트 목록 조회하기**

> operationId: `get-events-by-product-no`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
특정 상품을 포함하는 이벤트 목록 조회 API입니다

\
        1일 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/{eventKey}/`

**기획전 상세 조회하기 v2.0**

> operationId: `get-event-v2`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventKey` | path | ✅ | `string` | 기획전 번호 or 기획전 아이디 |
| `includeNonMemberCoupon` | query | - | `boolean` | "비로그인 고객의 상품 내 발급가능 쿠폰노출(default: false)" |
| `preview` | query | - | `boolean` | "기획전 미리보기 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
 기획전을 조회하는 API입니다.
 기획전에 상품진열을\
        \ 조회하는 API는 GET /display/events/{eventNo}/sections/{sectionNo} 를 이용바랍니다.
\
        \ eventNo와 eventId 둘 다 검색 가능합니다.

* 현재 진행중인 기획전만 조회되고, 그 이외(진행대기, 종료)에는\
        \ 조회되지 않습니다.

* 플랫폼별, 멤버그룹별, 멤버등급별에 따라 접근 불가능한 기획전은 조회되지 않습니다.

* 에러코드\
        : EVEC0001(기획전이 존재하지 않는 경우), EVEC0002(현재 진행중인 기획전이 아닌 경우), EVEC0003(접근할 수\
        \ 없는 기획전인 경우)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/ids/{eventId}/`

**기획전 ID로 상세 조회하기 v2.0**

> operationId: `get-event-by-id-v2`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventId` | path | ✅ | `string` | 기획전 ID |
| `includeNonMemberCoupon` | query | - | `boolean` | "비로그인 고객의 상품 내 발급가능 쿠폰노출(default: false)" |
| `preview` | query | - | `boolean` | "기획전 미리보기 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
ID로 기획전을 조회하는 API입니다.

기획전에 상품\
        진열을 조회하는 API는 GET /display/events/{eventNo}/sections/{sectionNo} 를 이용바랍니다\
        .

현재 진행중인 기획전만 조회되고, 그 이외(진행대기, 종료)에는 조회되지 않습니다.

플랫폼별, 멤버그룹별, 멤버등급별에\
        \ 따라 접근 불가능한 기획전은 조회되지 않습니다.

에러코드: EVEC0001(기획전이 존재하지 않는 경우), EVEC0002(현\
        재 진행중인 기획전이 아닌 경우), EVEC0003(접근할 수 없는 기획전인 경우)

 ### OAUTH2 참고사항
* [Oauth2\
        \ API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/events/{eventNo}/sections/{sectionNo}`

**기획전 상품진열 상품 조회**

> operationId: `get-event-section-products`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventNo` | path | ✅ | `string` | 기획전 번호 |
| `sectionNo` | path | ✅ | `string` | 상품진열 번호 |
| `order` | query | - | `string` | "정렬조건(SALE: 판매시작일 오름차순 - default, ADMIN_SETTING: 관리자 설정 순서, BEST_SELLER:\ |
| `direction` | query | - | `string` | "정렬 순서 ( DESC: 내림차순 , ASC: 오름차순 )" |
| `soldout` | query | - | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | - | `string` | "판매 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `includeStopProduct` | query | - | `boolean` | "판매중지 상품 포함 여부(default: false)" |
| `preview` | query | - | `boolean` | "기획전 미리보기 여부(default: false)" |
| `pageNumber` | query | - | `number` | 페이지 번호(default 1) |
| `pageSize` | query | - | `number` | 한 페이지당 상품 노출 수(default 10) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
기획전번호와 상품진열 번호로 해당 상품진열에 있는 상품을\
        \ 조회하는 API입니다.

size: 상품을 조회하는 최대 페이지 사이즈는 30입니다.

현재 진행중인 기획전만 조회되고,\
        \ 그 이외(진행대기, 종료)에는 조회되지 않습니다.

플랫폼별, 멤버그룹별, 멤버등급별에 따라 접근 불가능한 상품진열 상품은 조\
        회되지 않습니다.

에러코드: EVEC0001(기획전이 존재하지 않는 경우), EVEC0002(현재 진행중인 기획전이 아닌 경우\
        ), EVEC0003(접근할 수 없는 기획전인 경우)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## ProductSection — 상품 진열 관리

### 🟢 `GET` `/display/sections`

**상품 진열 리스트 조회하기**

> operationId: `get-sections`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 리스트를 반환합니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections`

**상품 진열 조회하기 (단일진열-페이징) v1.1**

> operationId: `get-section-v1-1`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionNo` | path | ✅ | `string` | 상품 진열 번호 (샵바이 어드민에서 입력한 상품 진열 번호입니다) |
| `by` | query | ✅ | `string` | "정렬 기준 (SALE_YMD:판매일자, ADMIN_SETTING: 랭킹순-관리자화면 기준, POPULAR:판\ |
| `direction` | query | ✅ | `string` | "정렬 순서 ( DESC: 내림차순 (DEFAULT), ASC: 오름차순 )" |
| `soldout` | query | ✅ | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | ✅ | `string` | "상품 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `pageNumber` | query | ✅ | `integer` | 페이지 번호 |
| `pageSize` | query | ✅ | `integer` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | ✅ | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasOptionValues` | query | ✅ | `boolean` | "옵션리스트 포함여부(default: false)" |
| `includeStopProduct` | query | ✅ | `boolean` | "판매중지 상품 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 번호(sectionNo)을 기준으로 상품 진열을\
        \ 조회하는 API입니다. 버전 v1.1 API입니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections/{sectionNo}`

**상품 진열 조회하기 (단일 진열) v2.0**

> operationId: `get-section-v2`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionNo` | path | ✅ | `string` | 상품 진열 번호 (샵바이 어드민에서 입력한 상품 진열 번호입니다) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 번호(sectionNo)를 기준으로 상품 진열을\
        \ 조회하는 API 입니다. `버전 v2.0` API 입니다.

상품에 대한 조회는 상품 진열 상세조회 API 를 통해 호출이 필\
        요합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections/{sectionNo}`

**디자인 섹션 조회하기**

> operationId: `get-sections-by-section-id`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionId` | path | ✅ | `string` | 진열ID |
| `by` | query | ✅ | `string` | "정렬 기준 ( ADMIN_SETTING: 랭킹순-관리자화면 기준, SALE: 판매량순, LOW_PRICE:\ |
| `soldout` | query | ✅ | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | ✅ | `string` | "상품 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `pageNumber` | query | ✅ | `integer` | "페이지 번호(default: 1)" |
| `pageSize` | query | ✅ | `integer` | "한 페이지당 노출 수(default: 10)" |
| `hasTotalCount` | query | ✅ | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasOptionValues` | query | ✅ | `boolean` | "옵션리스트 포함여부(default: false)" |
| `includeStopProduct` | query | ✅ | `boolean` | "판매중지 상품 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
디자인섹션을 조회하는 API입니다

'메인페이지_섹션영\
        역' 가이드 내 '섹션(section)' 관련 API설명 내용을 참고하시길 바랍니다.

https://shopby.works/guide/skin/dev-cover/main-section?lv=6
\
        
몰 생성 시에 프론트 화면에 노출될 상품 진열을 기본적으로 생성해주고 있습니다.

플랫폼 구분 타입(PC/Mobile Web)과\
        \ 분류코드(sectionId => SCPC0001, SPMO0001, SCPC0002, ...)로 생성합니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections/ids/{sectionId}`

**진열 ID로 상품 진열 조회하기 (단일 진열) v2.0**

> operationId: `get-section-by-id-v2`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionId` | path | ✅ | `string` | 상품 진열 ID (샵바이 어드민에서 입력한 상품 진열 ID입니다) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 ID(sectionId)를 기준으로 상품 진열을\
        \ 조회하는 API 입니다. `버전 v2.0` API 입니다.

상품에 대한 조회는 상품 진열 상세조회 API 를 통해 호출이 필\
        요합니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections/{sectionNo}/products`

**진열 번호로 상품 진열 내 상품 상세 조회하기 (단일진열-페이징)**

> operationId: `get-products-by-sectionNo`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionNo` | path | ✅ | `string` | 상품 진열 번호 |
| `by` | query | ✅ | `string` | "정렬 기준 (SALE_YMD:판매일자, ADMIN_SETTING: 랭킹순-관리자화면 기준, POPULAR:판\ |
| `direction` | query | ✅ | `string` | "정렬 순서 ( DESC: 내림차순 (DEFAULT), ASC: 오름차순 )" |
| `soldout` | query | ✅ | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | ✅ | `string` | "상품 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `pageNumber` | query | ✅ | `integer` | 페이지 번호 |
| `pageSize` | query | ✅ | `integer` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | ✅ | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasOptionValues` | query | ✅ | `boolean` | "옵션리스트 포함여부(default: false)" |
| `includeStopProduct` | query | ✅ | `boolean` | "판매중지 상품 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 번호(sectionNo)를 기준으로 상품 진열\
        \ 내 상품 목록을 조회하는 API입니다.

pageSize: 상품을 조회하는 최대 페이지 사이즈는 30입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/sections/ids/{sectionId}/products`

**진열 ID로 상품 진열 내 상품 상세 조회하기 (단일진열-페이징)**

> operationId: `get-section-v1`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `sectionId` | path | ✅ | `string` | 상품 진열 ID |
| `by` | query | ✅ | `string` | "정렬 기준 (SALE_YMD:판매일자, ADMIN_SETTING: 랭킹순-관리자화면 기준, POPULAR:판\ |
| `direction` | query | ✅ | `string` | "정렬 순서 ( DESC: 내림차순 (DEFAULT), ASC: 오름차순 )" |
| `soldout` | query | ✅ | `boolean` | "품절 상품 포함 여부(default: false)" |
| `saleStatus` | query | ✅ | `string` | "상품 상태 (판매대기, 판매중 상품만 조회: READY_ONSALE, 판매중인 상품만 조회: ONSALE,\ |
| `pageNumber` | query | ✅ | `integer` | 페이지 번호 |
| `pageSize` | query | ✅ | `integer` | 한 페이지당 노출 수 |
| `hasTotalCount` | query | ✅ | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `hasOptionValues` | query | ✅ | `boolean` | "옵션리스트 포함여부(default: false)" |
| `includeStopProduct` | query | ✅ | `boolean` | "판매중지 상품 포함 여부(default: false)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품 진열 ID(sectionId)를 기준으로 상품 진열\
        \ 내 상품 목록을 조회하는 API입니다.

pageSize: 상품을 조회하는 최대 페이지 사이즈는 30입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## Banner — 배너

### 🟢 `GET` `/display/banners/extraInfos`

**배너 추가 정보 조회하기**

> operationId: `get-banner-extra-infos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerSectionNo` | query | - | `number` | 배너 섹션 번호 |
| `bannerNos` | query | - | `string` | 배너 번호 리스트(쉼표로 구분하여 입력) |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
배너의 추가 정보를 조회하는 API 입니다.

* 배너\
        \ 섹션 번호 또는 배너 번호 리스트로 조회 가능합니다.

* 두 값을 모두 입력하는 경우, 배너 섹션 번호로 조회됩니다.

\
        \ ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/display/banners/{bannerSectionCodes}`

**배너 목록 조회하기(Code)**

> operationId: `get-banners`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerSectionCodes` | path | ✅ | `string` | "조회할 배너 섹션 코드(,로 구분한 배열) / 샵바이 어드민에서 `배너 코드` 항목으로 확인 가능합니다." |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
코드 정보들로 배너들을 조회하는 API입니다

5분 캐\
        시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token
## 화면\
        \ 예시
                
[![banner-list-img](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png)

---

### 🟢 `GET` `/display/banners/id/{bannerSectionIds}`

**배너 목록 조회하기(ID)**

> operationId: `get-banners-with-ids`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerSectionIds` | path | ✅ | `string` | "조회할 배너 섹션 ID(,로 구분한 배열) / 샵바이 어드민에서 입력한 섹션 ID 입니다" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
ID 정보로 배너들을 조회하는 API입니다

5분 캐시\
        하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token
## 화면\
        \ 예시
                
[![banner-list-img](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png)

---

## ProductInquiry — 상품 문의

### 🟢 `GET` `/products/inquiries`

**전체 상품 문의 목록 조회하기**

> operationId: `get-products-inquiries`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `pageNumber` | query | - | `number` | "페이지 번호 (default: 1)" |
| `pageSize` | query | - | `number` | "한 페이지당 노출 수 (default: 20)" |
| `searchType` | query | - | `string` | "검색어 기준 (내용: CONTENT, 상품명: PRODUCT_NAME)" |
| `searchKeyword` | query | - | `string` | 검색어 |
| `tagValueNos` | query | - | `number` | 태그값번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
전체 상품 문의 목록 조회하는 API입니다

Paging\
        \ 기능을 제공합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/inquiries/configurations`

**상품문의 게시판 설정 조회하기**

> operationId: `get-inquiries-configurations`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | - | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의 게시판 설정 조회하는 API입니다

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/inquiries/tags`

**상품문의 태그 전체 조회하기**

> operationId: `get-inquiries-tags`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의 태그 전체 조회하는 API입니다.

 ###\
        \ OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟡 `PUT` `/products/inquiries/{inquiryNo}`

**상품문의 수정하기**

> operationId: `put-product-inquiry`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNo` | path | ✅ | `string` | 문의 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의를 수정하는 API입니다

작성자 본인만 수정\
        \ 가능합니다

제목,내용,문의유형만 수정 가능합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔴 `DELETE` `/products/inquiries/{inquiryNo}`

**상품문의 삭제하기**

> operationId: `delete-product-inquiry`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNo` | path | ✅ | `string` | 상품문의 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의를 삭제하는 API입니다

작성자 본인만 삭제\
        \ 가능합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/inquiries`

**상품문의 목록 조회하기**

> operationId: `get-product-inquiries`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `startYmd` | query | - | `string` | "조회일 시작일(입력 안 할 경우 3개월, yyyy-MM-dd)" |
| `endYmd` | query | - | `string` | "조회일 종료일(입력 안 할 경우 오늘, yyyy-MM-dd)" |
| `answered` | query | - | `boolean` | "답변 유무 조건(답변이 달린 문의 목록: true, 답변이 달리지 않은 목록이거나 없을 경우 전체(default):\ |
| `isMyInquiries` | query | - | `boolean` | 내 문의만 조회 여부 |
| `tagValueNos` | query | - | `number` | 태그값번호 |
| `pageNumber` | query | - | `number` | 페이지 번호 |
| `pageSize` | query | - | `number` | 한 페이지당 노출 수 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품번호로 상품문의를 조회하는 API 입니다.

* 로\
        그인 했을 경우 accessToken을 추가해야 합니다

Paging 기능을 제공합니다.

조회 시작일/종료일을 입력하지 않는\
        \ 경우 최근 3개월간 상품문의 게시글이 조회됩니다.

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/inquiries`

**상품문의 등록하기**

> operationId: `post-product-inquiries`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의를 등록하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔴 `DELETE` `/products/inquiries/{inquiryNo}/report`

**상품문의 신고 취소하기**

> operationId: `cancel-product-inquiry-report`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNo` | path | ✅ | `string` | 문의 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의 신고 취소하는 API입니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🔵 `POST` `/products/{productNo}/inquiries/report`

**상품문의 신고하기**

> operationId: `post-product-inquiry-reports`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품문의를 신고하는 API 입니다.

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/products/{productNo}/inquiries/{inquiryNo}`

**상품문의 조회하기**

> operationId: `get-product-inquiry`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `productNo` | path | ✅ | `string` | 상품 번호 |
| `inquiryNo` | path | ✅ | `string` | 상품문의 번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | - | `string` | 회원 엑세스 토큰(OAuth2 스펙) |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
상품번호로 상품문의를 조회하는 API입니다

* 로그인\
        \ 했을 경우 accessToken을 추가해야 합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/profile/product-inquiries`

**내 상품문의 목록 조회하기**

> operationId: `get-profile-product-inquiries`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `hasTotalCount` | query | - | `boolean` | "목록 카운트 포함 여부(default: false)" |
| `startYmd` | query | - | `string` | "조회일 시작일(입력 안 할 경우 3개월, yyyy-MM-dd)" |
| `endYmd` | query | - | `string` | "조회일 종료일(입력 안 할 경우 오늘, yyyy-MM-dd)" |
| `answered` | query | - | `boolean` | "답변 유무 조건(답변이 달린 문의 목록: true, 답변이 달리지 않은 목록, 없을 경우 전체(default):\ |
| `pageNumber` | query | - | `number` | 페이지 번호 |
| `pageSize` | query | - | `number` | 한 페이지당 노출 수 |
| `searchType` | query | - | `string` | "검색어 기준 (내용: CONTENT, 상품명: PRODUCT_NAME, 전체: ALL)" |
| `searchKeyword` | query | - | `string` | 검색어 |
| `tagValueNos` | query | - | `integer` | 태그값번호 |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
내 상품문의를 조회하는 API입니다

Paging 기능\
        을 제공합니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/profile/product-inquiries/count`

**내 상품문의 횟수 조회하기**

> operationId: `get-product-inquiries-count`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `startYmd` | query | - | `string` | "조회일 시작일(입력 안 할 경우 3개월, yyyy-MM-dd)" |
| `endYmd` | query | - | `string` | "조회일 종료일(입력 안 할 경우 오늘, yyyy-MM-dd)" |
| `Version` | header | ✅ | `string` | API 버전 |
| `accessToken` | header | ✅ | `string` | 회원 엑세스 토큰 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
내 상품문의 횟수를 조회하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## SkinBanner

### 🟢 `GET` `/skin-banners`

**플랫폼 별 전체 스킨 배너 조회하기**

> operationId: `get-skin-banners`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerGroupCodes` | query | ✅ | `string` | "배너 그룹 코드 리스트 (, 구분값)" |
| `skinNo` | query | - | `number` | 스킨 번호 (미리보기의 경우에만 입력) |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
플랫폼 별 전체 스킨 배너 조회하는 API입니다

5분\
        \ 캐시하여 사용하고 있습니다. (cached)

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/skin-banners/groups-by-skin`

**사용 혹은 작업중 스킨 정보 및 배너 그룹 조회하기**

> operationId: `get-skin-banners-groups-by-skin`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `isPreview` | query | - | `boolean` | "미리보기 스킨 여부 (기본값 : false / false인 경우 사용중인 스킨의 배너 그룹 정보를 가져옴)" |
| `skinNo` | query | - | `number` | 스킨 번호 (미리보기의 경우에 필수 입력) |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
사용 혹은 작업중 스킨 정보 및 배너 그룹 조회하는 API입\
        니다

 ### OAUTH2 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

### 🟢 `GET` `/skin-banners/{bannerId}`

**스킨의 배너ID를 통해 배너리스트 조회하기**

> operationId: `get-skin-banners-by-banner-id`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerId` | path | ✅ | `string` | 배너 ID |
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
배너ID로 배너를 조회하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

## "[Sticker]"

### 🟢 `GET` `/stickers`

**스티커 목록 조회**

> operationId: `get-stickers`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `Version` | header | ✅ | `string` | API 버전 |
| `clientId` | header | ✅ | `string` | 쇼핑몰 클라이언트 아이디 |
| `platform` | header | ✅ | `string` | "PC - PC 웹 브라우저에서 접근 시, MOBILE_WEB - 모바일 웹 브라우저에서 접근 시, AOS -\ |

## 부가설명 및 특이사항
            
몰 별 전체 스티커 조회하는 API입니다

 ### OAUTH2\
        \ 참고사항
* [Oauth2 API](https://docs.shopby.co.kr/?url.primaryName=auth/#/OAUTH2/post-oauth2-token)\
        \ 를 사용하여 토큰을 발급받은 경우,
* 기존 accessToken 대신 Shop-By-Authorization 로 액세스 토큰을\
        \ 전달해야합니다.
* ex) Shop-By-Authorization : Bearer test-access-token

---

