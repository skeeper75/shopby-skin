---
title: "display-server"
description: "상품 전시(display) 관련 server API입니다."
version: "0.0.1-SNAPSHOT"
category: "Server API"
---

# display-server

:::info API 소개
 상품 전시(display) 관련 server API입니다.
:::

## 서버 정보

| 환경 | Base URL |
|------|----------|
| Production | `https://server-api.e-ncp.com` |

## 리소스 목록

| 리소스 | 설명 |
|--------|------|
| **Category** | 카테고리 |
| **ProductInquiry** | 상품 문의 |
| **Review** | 상품평 |

## API 엔드포인트

## Banner

### 🟢 `GET` `/banners`

**배너 조회하기**

> operationId: `get-banners`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keywords` | query | ✅ | `string` | 배너 코드 또는 ID |
| `keywordType` | query | ✅ | `string` | "검색 타입, (CODE, ID, NO)" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 코드 또는 ID 정보로 배너를 조회하는 API입니다
\
        
5분 캐시하여 사용하고 있습니다. (cached)
## 화면 예시
                
[![banner-list-img](https://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png?autox150)](http://image.toast.com/aaaaahb/api-description/display/[GET]%20display_banners_bannerSectionCodes%20%EB%B0%B0%EB%84%88%EB%AA%A9%EB%A1%9D%20%EC%A1%B0%ED%9A%8C%20(cached).png)

---

### 🔵 `POST` `/banners`

**' 배너 섹션 등록'**

> operationId: `배너 섹션 등록`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

' 배너 섹션 등록'

---

### 🔴 `DELETE` `/banners`

**' 배너 섹션 삭제'**

> operationId: `배너 섹션 삭제`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerNos` | query | ✅ | `string` | "삭제할 배너 번호^|[1, 2]" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

' 배너 섹션 삭제'

---

### 🟢 `GET` `/banners/extraInfo`

**배너 추가정보 조회하기**

> operationId: `get-banner-extraInfos`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerSectionNo` | query | - | `number` | 배너 섹션 번호 |
| `bannerNos` | query | - | `string` | "배너 번호 리스트(쉼표 구분, 최대 100개 동시 호출 가능)" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 추가정보를 조회 API입니다.

* 배너 섹션 번\
        호 또는 배너 번호 리스트로 조회 가능합니다. 두 값 중 하나 필수 입력. (두 값 모두 존재하는 경우, 배너 섹션 번호로 조회)

---

### 🟡 `PUT` `/banners/extraInfo`

**배너 추가정보 수정하기**

> operationId: `update-banner-extraInfo`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 추가정보를 수정하는 API입니다.

* 이미 삭제\
        된 섹션, 구좌의 배너이거나, 삭제된 배너의 경우에는 수정이 불가능합니다.

---

### 🔵 `POST` `/banners/extraInfo`

**배너 추가정보 등록하기**

> operationId: `create-banner-extraInfo`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 추가정보를 등록하는 API입니다.

* 이미 삭제\
        된 섹션, 구좌의 배너이거나, 삭제된 배너의 경우에는 생성이 불가능합니다.

---

### 🔴 `DELETE` `/banners/extraInfo`

**배너 추가정보 삭제하기**

> operationId: `delete-banner-extraInfo`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerNos` | query | ✅ | `string` | 삭제할 배너 번호 리스트(쉼표 구분) |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 추가정보를 삭제하는 API입니다.

* 이미 삭제\
        된 섹션, 구좌의 배너이거나, 삭제된 배너의 경우에는 삭제가 불가능합니다.

---

### 🟢 `GET` `/banners/groups`

**배너 그룹 조회하기**

> operationId: `get-simple-groups`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
배너 그룹 조회 API입니다.

* 배너 섹션 번호 또\
        는 배너 번호 리스트로 조회 가능합니다. 두 값 중 하나 필수 입력. (두 값 모두 존재하는 경우, 배너 섹션 번호로 조회)

---

### 🟢 `GET` `/banners/simple-infos`

**헤드리스 배너 조회**

> operationId: `헤드리스 배너 조회`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `keywordInfo.keywords` | query | - | `string` | 검색어(쉼표 구분)^|id |
| `keywordInfo.keywordType` | query | - | `string` | "검색어 타입 : (BANNER_GROUP_NO, BANNER_NO, BANNER_ID, BANNER_CODE)^|BANNER_ID" |
| `lastBannerNo` | query | - | `number` | 조회할 다음 배너 번호^|100 |
| `size` | query | - | `number` | 페이지당 조회 수^|30 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

헤드리스 배너 조회

---

### 🟡 `PUT` `/banners/{bannerNo}`

**' 배너 섹션 수정'**

> operationId: `배너 섹션 수정`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `bannerNo` | path | ✅ | `string` | "" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

' 배너 섹션 수정'

---

## Category — 카테고리

### 🟢 `GET` `/categories`

**표준 카테고리 조회하기**

> operationId: `get-categories`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
표준 카테고리 조회하는 API입니다

---

### 🟢 `GET` `/categories/display-categories`

**전시 카테고리 조회하기**

> operationId: `get-display-categories`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
전시 카테고리 조회하는 API입니다

---

### 🟢 `GET` `/categories/display-categories/tree`

**전시카테고리 트리 조회하기**

> operationId: `get-display-categories-tree`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `hasProductCount` | query | - | `boolean` | 연결된 상품 수 포함 여부^|true |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
전시카테고리 트리를 조회하는 API입니다

---

## Event

### 🟢 `GET` `/events`

**기획전 검색**

> operationId: `get-events`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `searchDateType` | query | - | `string` | 조회 기간 타입^|REGISTER_YMDT |
| `datePeriod.startYmd` | query | - | `string` | 조회 시작일^|2025-10-16 |
| `datePeriod.endYmd` | query | - | `string` | 조회 종료일^|2026-01-16 |
| `searchType` | query | - | `string` | 검색어 타입^|EVENT_NAME |
| `keyword` | query | - | `string` | 검색어^|검색어 |
| `progressStatus` | query | - | `string` | "진행상태^|READY,ING" |
| `eventType` | query | - | `string` | 기획전 유형^|GENERAL |
| `page` | query | - | `number` | 시작 페이지^|1 |
| `size` | query | - | `number` | 조회 수^|30 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |
| `App-Key` | header | - | `string` | 어플리케이션 키^|test-app-key |

## 부가설명 및 특이사항
            
기획전을 검색하는 API입니다

* 검색 기간 타입:\
        \ - REGISTER_YMDT: 등록일
- DISPLAY_YMDT: 전시기간

* 검색어 타입: - EVENT_NAME: 기획\
        전명
- EVENT_NO: 기획전 번호
- ADMIN: 담당자
- EVENT_ID: 기획전 Id

* 기획전 유형: - GENERAL:\
        \ 일반
- EXTERNAL: 외부링크

* 진행상태: - ALL: 전체
- READY: 진행대기
- ING: 진행중
-\
        \ END: 진행종료

---

### 🟢 `GET` `/events/{eventNo}`

**기획전 단건 조회**

> operationId: `get-event-detail`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `eventNo` | path | ✅ | `number` | 기획전 번호^|1 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |
| `App-Key` | header | - | `string` | 어플리케이션 키^|test-app-key |

기획전 상세 정보를 조회하는 API입니다.

---

## ProductInquiry — 상품 문의

### 🟢 `GET` `/inquiry`

**상품문의 조회하기**

> operationId: `get-inquiry`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `startYmd` | query | - | `string` | 조회 시작일 (입력 안 할 경우 - 3개월 전)^|yyyy-MM-dd |
| `endYmd` | query | - | `string` | 조회 종료일 (입력 안 할 경우 - 오늘)^|yyyy-MM-dd |
| `replyYn` | query | - | `string` | 답변여부 (입력 안 할 경우 - 전체조회)^|Y |
| `inquiryNo` | query | - | `number` | 문의번호 (입력 안 할 경우 - 전체조회)^|11 |
| `searchDateType` | query | - | `string` | "기간 검색 종류 (DEFAULT: REGISTER_YMDT) (등록일: REGISTER_YMDT, 답변일:\ |
| `member.type` | query | - | `string` | "회원검색조건 - 회원검색타입 (NAME, ID, NO)^|ID" |
| `member.keyword` | query | - | `string` | 회원검새조건 - 회원ID or 회원명^|shopyById |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |
| `App-Key` | header | - | `string` | 어플리케이션 키^|test-app-key |

## 부가설명 및 특이사항
            
상품문의 조회하는 API입니다

---

### 🟢 `GET` `/inquiry/replies`

**상품문의 답변 조회하기**

> operationId: `get-inquiry-replies`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNos` | query | - | `number` | "답글이 달린 문의 번호 (최대 100개까지 조회 가능)^|[10234991]" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |
| `App-Key` | header | - | `string` | 어플리케이션 키^|test-app-key |

## 부가설명 및 특이사항
            
상품문의에 등록된 답변을 조회하는 API입니다

* 상\
        품문의 조회 API를 통해 조회된 문의의 답변을 조회하고자 하는 경우 inquiryNos 를 이용하여 조회

---

### 🟡 `PUT` `/inquiry/{inquiryNo}/display-status`

**상품 문의 전시 상태 변경하기**

> operationId: `put-inquiry-display-status`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNo` | path | ✅ | `number` | 상품 문의 번호^|10000 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |
| `App-Key` | header | - | `string` | 어플리케이션 키^|test-app-key |

## 부가설명 및 특이사항
            
상품 문의 전시 상태를 변경하는 API입니다

---

### 🔵 `POST` `/inquiry/{inquiryNo}/reply`

**상품문의 답변 등록하기**

> operationId: `post-inquiry-reply`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `inquiryNo` | path | ✅ | `number` | 상품 문의 번호^|134 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품문의 답변 등록하는 API입니다

---

## Review — 상품평

### 🟢 `GET` `/reviews`

**상품평 조회하기**

> operationId: `get-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `startYmd` | query | - | `string` | 작성일시 시작 (입력 안 할 경우 3개월 전)^|yyyy-MM-dd |
| `endYmd` | query | - | `string` | 작성일시 끝 (입력 안 할 경우 오늘)^|yyyy-MM-dd |
| `page` | query | - | `number` | 페이지 번호^|1 |
| `size` | query | - | `number` | 조회 수^|10 |
| `reviewNo` | query | - | `number` | 상품평 번호^|123 |
| `searchAfter` | query | - | `string` | "검색 기준 값(lastId)^|2022-01-01T00:00:00,123456" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 조회하는 API입니다

검색 방식

* 아래\
        \ 두 가지 검색방식을 모두 지원합니다.

* (기존) paging search : page 값 필수 (최초 조회 시 생략 가능\
        )

* (신규) keySet search : searchAfter 필수 (최초 조회 시 생략 가능. response의 lastId\
        \ 값 사용.)

---

### 🔵 `POST` `/reviews`

**상품평 검색하기**

> operationId: `search-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 검색 API입니다.
배송 파트너정보로 호출하는 경\
        우 모든 상품의 상품평이 검색되며 그 이외에는 파트너가 등록한 상품의 상품평만 조회됩니다.

검색 방식

* 아래 두 가지 검\
        색방식을 모두 지원합니다.

* (기존) paging search : page 값 필수 (최초 조회 시 생략 가능)

* (신\
        규) keySet search : searchAfter 필수 (최초 조회 시 생략 가능. response의 lastId 값 사용.)

---

### 🟡 `PUT` `/reviews/best-review`

**상품평 베스트 리뷰 일괄 변경**

> operationId: `update-best-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 베스트 리뷰 일괄 변경 API입니다.
최대 1000개\
        까지 일괄 수정 가능합니다.

---

### 🔵 `POST` `/reviews/external-site`

**외부 상품평 등록하기**

> operationId: `create-reviews-external-site`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
외부 상품평 등록 API입니다.

* 상품평 등록은 한\
        번에 최대 100개까지 요청 가능합니다.

---

### 🟡 `PUT` `/reviews/extraJson`

**상품평 extraJson 일괄 변경**

> operationId: `update-review-extraJson`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 extraJson 일괄 변경 API입니다.
최대\
        \ 1000개까지 일괄 수정 가능합니다.

---

### 🟡 `PUT` `/reviews/product-reviews`

**상품평 수정하기**

> operationId: `update-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | ✅ | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 수정 API입니다.

* 상품평 수정은 한번에\
        \ 최대 100개까지 요청 가능합니다.

* 상품 평점을 소수점으로 요청시 소수점 아래부분은 버림처리 됩니다.

---

### 🔵 `POST` `/reviews/product-reviews`

**상품평 등록하기**

> operationId: `create-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | ✅ | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 등록 API입니다.

* 상품평 등록은 한번에\
        \ 최대 100개까지 요청 가능합니다.

* 상품 평점을 소수점으로 요청시 소수점 아래부분은 버림처리 됩니다.

---

### 🔴 `DELETE` `/reviews/product-reviews`

**상품평 삭제하기**

> operationId: `delete-reviews`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `reviewNos` | query | ✅ | `string` | "삭제할 상품 리뷰 번호 리스트(쉼표 구분)^|1,2" |
| `registerNos` | query | ✅ | `string` | "작성자 번호 리스트(쉼표 구분)^|3,4" |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | ✅ | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 삭제 API입니다.

* 상품평 삭제는 한번에\
        \ 최대 100개까지 요청 가능합니다.

* 상품평번호와 작성자번호를 콤마 기준으로 매칭시켜 요청바랍니다.

---

### 🟡 `PUT` `/reviews/status`

**상품평 전시상태 일괄 변경**

> operationId: `update-review-status`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
상품평 전시상태 일괄 변경 API입니다.
최대 1000개\
        까지 일괄 수정 가능합니다.

---

## Sticker

### 🟢 `GET` `/stickers`

**스티커 목록 조회하기**

> operationId: `search-stickers`

**Parameters**

| 이름 | 위치 | 필수 | 타입 | 설명 |
|------|------|:----:|------|------|
| `page` | query | - | `number` | 페이지 번호^|1 |
| `size` | query | - | `number` | 한 페이지에 조회되는 상품평 갯수^|30 |
| `systemKey` | header | ✅ | `string` | "\n 시스템 키 (외부시스템 연동을 위한 server API 호출 키)\n - 발급경로: 워크스페이스> 셀러\ |
| `Authorization` | header | - | `string` | "\n \n 인증토큰 (Bearer 전달받은 토큰)\n \n - 형식: Bearer + 발급받은 access_token\n\ |
| `mallKey` | header | - | `string` | " 외부 API 연동키 (외부시스템연동 코드)\n \n ※ server API 호출하는 기존 방식으로, 향후\ |
| `Version` | header | ✅ | `string` | API 버전^|1.0 |

## 부가설명 및 특이사항
            
몰에 등록된 스티커 목록 조회 API입니다.

---

