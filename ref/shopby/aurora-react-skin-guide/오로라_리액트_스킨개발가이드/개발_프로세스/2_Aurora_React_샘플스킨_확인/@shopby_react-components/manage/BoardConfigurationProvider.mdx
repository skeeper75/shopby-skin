---
title: "BoardConfigurationProvider"
depth: 5
path: "오로라 리액트 스킨개발가이드 > 개발 프로세스 > 2️⃣Aurora React 샘플스킨 확인 > @shopby/react-components > manage > BoardConfigurationProvider"
---

# BoardConfigurationProvider

## 속성

| 이름 | 필수여부 | 타입 | 기본 값 | 설명 |
| --- | --- | --- | --- | --- |
| children | true | ReactNode |  |  |

## 상태

* boardConfigurationState 는 useBoardConfigurationContextState 를 호출해 사용한다.

``` typescript
const boardConfigurationState = useBoardConfigurationContextState();
```

| 이름 | 필수여부 | 타입 | 기본 값 | 설명 |
| --- | --- | --- | --- | --- |
| boardConfig | false | object | - | [response](https://docs.shopby.co.kr/?url.primaryName=manage/#/Board/get-board-config) 타입과 동일 |

## 액션

* boardConfigurationAction 은 useBoardConfigurationContextAction 를 호출해 사용한다.

``` typescript
const boardConfigurationAction = useBoardConfigurationContextAction();
```

| 이름 | 인자타입 | 필수여부 | 기본값 | 설명 | 관리하는 상태 |
| --- | --- | --- | --- | --- | --- |
| fetchBoardConfiguration | - | - | - | 게시판 설정을 조회함. | boardConfigurationState.boardConfig |

## 사용예시

``` jsx
const App = () => {
    return (
        
            
        
    )
}
```